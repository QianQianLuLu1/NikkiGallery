# 无限暖暖相册管理工具 - 架构优化与功能升级计划

## 一、项目现状总览

| 项目 | 说明 |
|------|------|
| 项目名称 | 无限暖暖相册管理工具 (WXNN Photo Manager) |
| 技术栈 | Electron 28 + React 18 + TypeScript 5.3 + Vite 5 + Zustand 4.5 + TailwindCSS 3.4 + Sharp 0.33 |
| 数据库 | SQLite (better-sqlite3) |
| 项目路径 | `h:\45001\Documents\WXNN XiangCe\wxnn-photo-manager\` |
| 当前版本 | v2.0.0 |
| 目标版本 | v2.1.0 |

### 1.1 代码审查问题汇总

| 类别 | 严重 | 中等 | 改进 |
|------|------|------|------|
| 安全漏洞 | 5 | 2 | - |
| 内存泄漏 | 7 | 1 | - |
| 状态管理 | 4 | 5 | - |
| 错误处理 | 6 | 3 | - |
| 类型安全 | 3 | 4 | - |
| Promise rejection | 3 | 2 | - |
| 组件设计 | 2 | 1 | - |
| 重复代码 | 4 | 3 | - |
| 性能问题 | 4 | 4 | - |
| 可访问性 | 4 | 4 | - |
| 功能改进 | - | - | 30+ |

---

## 二、安全漏洞修复（P0 紧急）

### 2.1 IPC 暴露原始 SQL 接口（SQL 注入漏洞）

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 2.1.1 | 移除 `db:query` 和 `db:execute` 通用 SQL IPC 通道 | P0 |
| 2.1.2 | 改为提供具体领域方法（`media:list`、`media:search`、`media:filter` 等） | P0 |
| 2.1.3 | 主进程内部统一使用参数化查询，禁止拼接 SQL | P0 |

**问题位置：** [src/main/index.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/index.ts#L124-L130) 第 124-130 行、[src/main/preload.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/preload.ts#L7-L10) 第 7-10 行

**风险：** 渲染进程被 XSS 攻击后可执行任意 SQL（包括 `DROP TABLE`、`DELETE FROM`），消解了沙箱意义。

### 2.2 `media://` 协议路径遍历漏洞

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 2.2.1 | 限制 `media://` 协议可访问路径必须在已索引的媒体目录内 | P0 |
| 2.2.2 | 维护允许路径白名单，拒绝访问白名单外文件 | P0 |
| 2.2.3 | 移除 `bypassCSP: true` 配置，评估是否真的需要 | P1 |

**问题位置：** [src/main/index.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/index.ts#L678-L707) 第 678-707 行

**风险：** 渲染进程可构造 `media://x?path=../../../etc/passwd` 读取任意系统文件。

### 2.3 `shell:openExternal` 未校验 URL

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 2.3.1 | 仅允许 `http:`/`https:` 协议，拒绝 `file:`、`smb:` 等危险协议 | P0 |

**问题位置：** [src/main/index.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/index.ts#L201-L204) 第 201-204 行

### 2.4 `eval()` 执行 ffprobe 输出

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 2.4.1 | 移除 `eval(videoStream.r_frame_rate)`，改用字符串解析 `"num/den"` 后除法计算 | P0 |

**问题位置：** [src/main/file-operations.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/file-operations.ts#L276) 第 276 行

### 2.5 `category:update` 字段名注入

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 2.5.1 | 维护允许更新的字段白名单 `['name', 'icon', 'color', 'parent_id']`，过滤 key | P1 |

**问题位置：** [src/main/index.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/index.ts#L439-L455) 第 439-455 行

---

## 三、数据一致性 Bug 修复（P0 紧急）

### 3.1 `INSERT OR REPLACE` 覆盖用户数据

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 3.1.1 | 改用 `INSERT INTO ... ON CONFLICT(file_path) DO UPDATE SET` 仅更新文件元数据字段 | P0 |
| 3.1.2 | 保留用户编辑字段（tags、category_id、rating、is_favorite、notes） | P0 |

**问题位置：** [src/main/scanner/index.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/scanner/index.ts#L306-L331) 第 306-331 行

**风险：** 每次增量扫描时用户编辑的标签、分类、评分、收藏、备注被重置为默认值，严重数据丢失。

### 3.2 `data:clear` 未使用事务

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 3.2.1 | 用 `db.transaction(() => { ... })()` 包裹所有清理操作 | P0 |
| 3.2.2 | 清理所有相关表（media_files、scan_history、media_tags、filter_presets、watermark_templates、edit_history） | P0 |
| 3.2.3 | 清理缩略图缓存目录 | P1 |

**问题位置：** [src/main/index.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/index.ts#L217-L229) 第 217-229 行

### 3.3 `JSON.parse` 无防护

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 3.3.1 | `tag:add`/`tag:remove` 中 `JSON.parse(row.tags)` 用 try/catch 包裹，失败回退空数组 | P0 |
| 3.3.2 | 全局搜索所有 `JSON.parse` 调用，补充错误处理 | P1 |

**问题位置：** [src/main/index.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/index.ts#L509) 第 509 行、第 533 行

### 3.4 `category:reorder` 循环 UPDATE 无事务

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 3.4.1 | 用 `db.transaction(() => { for (...) stmt.run(...) })()` 包裹 | P1 |

**问题位置：** [src/main/index.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/index.ts#L468-L480) 第 468-480 行

---

## 四、内存泄漏修复（P0/P1）

### 4.1 Scanner progress 监听器累积泄漏

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 4.1.1 | 在类构造时注册一次监听器，或每次扫描前 `removeAllListeners('progress')` | P0 |

**问题位置：** [src/main/index.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/index.ts#L133-L144) 第 133-144 行

### 4.2 渲染进程 IPC 监听未取消订阅

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 4.2.1 | `App.tsx` 保存 `onUpdated` 返回的 unsubscribe 函数，在 cleanup 中调用 | P0 |
| 4.2.2 | `ScanButton.tsx` 保存 `onProgress` 返回值，避免重复注册 | P0 |
| 4.2.3 | `useTheme.ts` 补充 `electronAPI.theme.onChange` 的取消订阅 | P1 |
| 4.2.4 | `preload.ts` 中 `scanner.onProgress` 和 `theme.onChange` 返回 unsubscribe 函数 | P1 |
| 4.2.5 | 移除 `App.tsx` 与 `GalleryPage.tsx` 重复注册的 `onUpdated` 监听 | P1 |

**问题位置：** [src/renderer/App.tsx](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/renderer/App.tsx#L26-L30) 第 26-30 行、[src/renderer/components/scanner/ScanButton.tsx](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/renderer/components/scanner/ScanButton.tsx#L13-L15) 第 13-15 行

### 4.3 setTimeout 未清理

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 4.3.1 | `EditorPage.tsx`、`GalleryPage.tsx`、`CategoriesPage.tsx` 的 `showMessage` 使用 `useRef` 保存 timer id | P1 |
| 4.3.2 | 在组件卸载的 `useEffect` 清理函数中 `clearTimeout` | P1 |

**问题位置：** [src/renderer/pages/EditorPage.tsx](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/renderer/pages/EditorPage.tsx#L111-L114) 第 111-114 行等

### 4.4 Image 加载未提供取消机制

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 4.4.1 | `useImageProcessor.ts` 在 cleanup 中设置 `img.onload = null; img.onerror = null` 阻断回调 | P0 |
| 4.4.2 | `FilterPanel.tsx` 同样处理 Image 竞态问题 | P1 |

**问题位置：** [src/renderer/hooks/useImageProcessor.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/renderer/hooks/useImageProcessor.ts#L49-L70) 第 49-70 行

---

## 五、状态管理与竞态条件修复（P0/P1）

### 5.1 `pushHistory` 闭包陷阱

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 5.1.1 | 将 `historyIndex` 通过 `setHistoryIndex` 的 updater 同步管理，或使用 ref 跟踪最新值 | P0 |
| 5.1.2 | 限制历史记录上限 50 步，LRU 淘汰 | P1 |

**问题位置：** [src/renderer/pages/EditorPage.tsx](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/renderer/pages/EditorPage.tsx#L74-L81) 第 74-81 行

**风险：** 快速连续拖动多个 slider 时 `historyIndex` 闭包未更新，`slice(0, historyIndex + 1)` 截断错误，导致历史记录错乱。

### 5.2 EXIF 加载无竞态保护

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 5.2.1 | `DetailPage.tsx` EXIF 加载添加 token 检查，避免旧 Promise 覆盖新 EXIF | P0 |
| 5.2.2 | `preview.html` 第 2209 行 EXIF 解析同样添加请求 ID 校验 | P1 |

**问题位置：** [src/renderer/pages/DetailPage.tsx](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/renderer/pages/DetailPage.tsx#L27-L30) 第 27-30 行

### 5.3 Shift 多选越界 bug

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 5.3.1 | `handleGridItemClick` Shift 选择逻辑增加边界检查，`selectedMediaIds` 为空时直接选中当前项 | P0 |

**问题位置：** [src/renderer/pages/GalleryPage.tsx](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/renderer/pages/GalleryPage.tsx#L457-L474) 第 457-474 行

**风险：** `selectedMediaIds` 为空时 `findIndex` 返回 -1，循环从 -1 开始，`filteredFiles[-1].id` 抛错导致运行时崩溃。

### 5.4 `mediaStore.updateMediaFile` 副作用混入 reducer

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 5.4.1 | 拆分为 action 函数，先 await IPC 再 set，避免本地状态与数据库不一致 | P1 |
| 5.4.2 | `db.execute` 调用补充 await 与错误捕获 | P1 |

**问题位置：** [src/renderer/stores/mediaStore.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/renderer/stores/mediaStore.ts#L112-L138) 第 112-138 行

---

## 六、错误处理与类型安全（P0/P1）

### 6.1 全局 IPC 调用未 try-catch

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 6.1.1 | `EditorPage.tsx` 的 `handleExportPreset`、`handleImportPreset`、`handleSavePreset` 补充 try-catch | P0 |
| 6.1.2 | `GalleryPage.tsx` 的 `handleSaveAs`、`handleCopy`、`handleMove`、`handleBatchExport`、`handleBatchMove`、`handleBatchWatermark` 补充 try-catch | P0 |
| 6.1.3 | `DetailPage.tsx` 的 `handleDelete` 中 `db.execute` 失败处理 | P0 |
| 6.1.4 | `CategoriesPage.tsx` 的 `handleCreate` 等 IPC reject 处理 | P1 |
| 6.1.5 | `WatermarkPanel.tsx` 的 `handleSaveTemplate` 补充 try-catch | P1 |
| 6.1.6 | `SettingsPage.tsx` 的 `clearData` 补充 try-catch 与用户反馈 | P1 |

### 6.2 类型安全问题

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 6.2.1 | `mediaStore.ts` 第 74 行 `editHistory: any[]` 改为具体类型 | P1 |
| 6.2.2 | `EditorPage.tsx` 导入预设添加运行时 schema 校验（zod） | P1 |
| 6.2.3 | `useImageProcessor.ts` 消除 `key as keyof FilterParams` 断言链，处理未知字段 | P1 |
| 6.2.4 | `Sidebar.tsx` 第 43 行 `navigateTo(item.id as any)` 改为 `ViewLevel` 联合类型 | P2 |
| 6.2.5 | `useFilterEngine.ts` 消除双重断言 `as unknown as Record<string, number>` | P2 |

### 6.3 `editor:importPreset` 无结构验证

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 6.3.1 | 添加 zod 或手动 schema 验证，校验 `name`、`category`、`params` 字段存在且类型正确 | P1 |

**问题位置：** [src/main/index.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/index.ts#L283-L301) 第 283-301 行

---

## 七、架构重构（P1/P2）

### 7.1 拆分 Application God Class

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 7.1.1 | 拆分 `IpcRouter` 模块，按领域分组（media、editor、watermark、category、tag、video、file、settings） | P1 |
| 7.1.2 | 拆分 `MediaProtocolHandler` 模块，独立处理 `media://` 协议 | P1 |
| 7.1.3 | 拆分 `ThemeManager` 模块 | P2 |
| 7.1.4 | 拆分 `DatabaseMaintenanceService` 模块，独立处理清理修复逻辑 | P2 |
| 7.1.5 | 拆分 `ScanScheduler` 模块，独立处理启动扫描调度 | P2 |

**问题位置：** [src/main/index.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/index.ts#L39-L842) 第 39-842 行（单一类 800+ 行，`setupIPC` 方法 530+ 行）

### 7.2 建立 Repository 层

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 7.2.1 | 建立 `PresetRepository`，封装滤镜预设 CRUD | P1 |
| 7.2.2 | 建立 `CategoryRepository`，封装分类 CRUD | P1 |
| 7.2.3 | 建立 `TagRepository`，封装标签 CRUD | P1 |
| 7.2.4 | 建立 `WatermarkRepository`，封装水印模板 CRUD | P2 |
| 7.2.5 | 建立 `MediaRepository`，封装媒体文件 CRUD | P2 |
| 7.2.6 | 移除 `DatabaseManager.getDatabase()`，所有 SQL 操作通过 Repository | P2 |

**问题位置：** [src/main/database/connection.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/database/connection.ts#L196-L198) 第 196-198 行

### 7.3 拆分 `FileOperations` 类

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 7.3.1 | 拆分 `FileService`（复制/移动/删除/重命名） | P1 |
| 7.3.2 | 拆分 `VideoService`（转码/帧截图/元数据） | P1 |
| 7.3.3 | 拆分 `ExifService`（EXIF 解析） | P2 |
| 7.3.4 | 拆分 `WatermarkService`（水印 SVG 生成与批量合成） | P2 |

### 7.4 统一 IPC 返回结构

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 7.4.1 | 定义统一 `IpcResult<T>` 类型：`{ success: boolean; data?: T; message?: string }` | P1 |
| 7.4.2 | 所有 IPC 处理器统一返回 `IpcResult`，消除三种返回风格混用 | P1 |

### 7.5 数据库迁移版本管理

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 7.5.1 | 引入 `user_version` pragma 和顺序迁移脚本 | P2 |
| 7.5.2 | 支持表结构变更（新增字段、修改索引） | P2 |

### 7.6 引入日志系统

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 7.6.1 | 引入 electron-log 或 winston，支持日志级别与文件输出 | P2 |
| 7.6.2 | 替换全项目 `console.log`/`console.error`，消除静默吞错 | P2 |

### 7.7 应用退出时关闭数据库

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 7.7.1 | 在 `app.on('before-quit')` 中调用 `dbManager.close()` | P1 |
| 7.7.2 | 确保 WAL 文件 checkpoint，避免数据丢失 | P1 |

---

## 八、渲染层组件重构（P1/P2）

### 8.1 拆分 `EditorPage.tsx`（587 行）

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 8.1.1 | 提取 `useEditHistory` hook（历史栈管理） | P1 |
| 8.1.2 | 提取 `useEditorShortcuts` hook（快捷键监听） | P1 |
| 8.1.3 | 拆分 `EditorToolbar` 组件（撤销/重做/重置/对比/保存/另存为/取消） | P1 |
| 8.1.4 | 拆分 `EditorTabs` 组件（7 个 tab 的渲染） | P1 |
| 8.1.5 | 拆分 `ShortcutsModal` 组件（快捷键说明弹窗） | P2 |

### 8.2 拆分 `GalleryPage.tsx`（961 行）

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 8.2.1 | 提取 `useFileOperations` hook（文件操作） | P1 |
| 8.2.2 | 提取 `useBatchOperations` hook（批量操作） | P1 |
| 8.2.3 | 拆分 `ListView`、`TimelineView`、`MasonryView` 独立组件 | P1 |
| 8.2.4 | 拆分 `RenameDialog`、`WatermarkDialog` 独立组件 | P2 |
| 8.2.5 | 提取 `getContextMenuItems` 到工具函数 | P2 |

### 8.3 消除重复代码

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 8.3.1 | 提取 `mergeFilterParams(params, filter, intensity)` 工具函数，消除三处重复 | P0 |
| 8.3.2 | 提取 `toFileUrl` 到 `utils/file.ts`，`WatermarkPanel` 复用 `mediaStore` 导出版本 | P1 |
| 8.3.3 | 提取 `formatFileSize` 到 `utils/format.ts`，统一四处实现 | P1 |
| 8.3.4 | 提取 `formatDate` 到 `utils/date.ts`，统一两处实现 | P1 |
| 8.3.5 | 提取 `<Toast>` 组件 + `useToast` hook，消除三处消息提示重复 | P2 |
| 8.3.6 | 提取 SVG 图标到 `icons/` 目录，消除内联重复 | P2 |
| 8.3.7 | 封装 `useRefreshMedia()` hook，消除 10+ 处 `loadMediaFromDatabase` 调用重复 | P2 |

### 8.4 性能优化

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 8.4.1 | `useImageProcessor.ts` `renderPreview` 添加 50-100ms 防抖 | P0 |
| 8.4.2 | `GalleryPage.tsx` 搜索输入添加 200-300ms 防抖 | P0 |
| 8.4.3 | `Histogram.tsx` 与 `renderPreview` 同步防抖 | P1 |
| 8.4.4 | `FilterPanel.tsx` `generateThumbnails` 改为并行处理（限制并发数） | P1 |
| 8.4.5 | `EditorPage.tsx` 14 个 SliderControl 的 `onChange` 使用 `useCallback` memo | P1 |
| 8.4.6 | `FullscreenViewer.tsx` `filteredFiles` 使用 `useMemo` | P1 |
| 8.4.7 | `Sidebar.tsx` 三次遍历 `mediaFiles` 合并为一次 `useMemo` | P2 |
| 8.4.8 | `uiStore` 添加 `zustand/middleware/persist`，持久化用户偏好 | P2 |

---

## 九、主进程性能与可靠性（P1/P2）

### 9.1 同步 fs 操作改异步

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 9.1.1 | `index.ts` 第 290、312、326、605 行 `fs.readFileSync`/`writeFileSync` 改为 `fs.promises.*` | P1 |
| 9.1.2 | `file-operations.ts` 第 88、114、142、200、232、246、419、584 行同步 fs 改异步 | P1 |
| 9.1.3 | `scanner/index.ts` 第 175、214、232 行同步 fs 改异步 | P1 |
| 9.1.4 | `file-operations.ts` 第 403 行 `showOpenDialogSync` 改为 `showOpenDialog` | P1 |

**风险：** 同步 fs 操作阻塞 Electron 主进程，导致 UI 卡顿、鼠标点击无响应。

### 9.2 `fs.renameSync` 跨盘移动失败

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 9.2.1 | 使用 `fs-extra` 的 `moveSync` 或先 `copyFileSync` 再 `unlinkSync` | P1 |

**问题位置：** [src/main/file-operations.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/file-operations.ts#L114) 第 114、142 行

### 9.3 视频文件流式响应

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 9.3.1 | `media://` 协议对视频文件使用 `fs.createReadStream` 流式响应，避免 OOM | P1 |

**问题位置：** [src/main/index.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/index.ts#L696) 第 696 行

### 9.4 缩略图生成并发处理

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 9.4.1 | 使用 `p-limit` 等并发控制库，限制 4-8 并发 | P1 |
| 9.4.2 | 抽取 `processThumbnailForRow(row)` 公共方法，消除 `generateThumbnailsForUnprocessed` 与 `cleanupAndRepairDatabase` 重复 | P2 |

### 9.5 视频元数据并发读取

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 9.5.1 | 扫描时批量并发读取视频元数据 | P2 |
| 9.5.2 | 或扫描阶段跳过元数据读取，扫描完成后异步补充 | P2 |

### 9.6 ffmpeg 超时处理

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 9.6.1 | `extractVideoFrame` 包装 `Promise.race` 加 30 秒超时 | P1 |
| 9.6.2 | 修复 `.size()` 与 `-vf scale` 重复设置冲突 | P1 |

### 9.7 `this.mainWindow!` 非空断言

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 9.7.1 | 改为 `this.mainWindow?.webContents?.send(...)` 并判空 | P1 |

### 9.8 缩略图缓存清理

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 9.8.1 | 启动时或定期清理无对应数据库记录的孤儿缩略图 | P2 |

---

## 十、UI/UX 改进与功能丰富（P1/P2）

### 10.1 设置页 - 开发者介绍与社交媒体（P0 必做）

**需求：** 在设置选项的二级界面中固定区域，居中显示开发者介绍，正下方并列三个社交媒体图标按钮。

#### 10.1.1 开发者介绍区域

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 10.1.1.1 | 在设置页"关于"卡片内新增固定区域，居中显示两行文本 | P0 |
| 10.1.1.2 | 文本内容："开发者：纤璐不会玩摄影（全网同名）"，字体加粗 | P0 |
| 10.1.1.3 | 文本居中对齐，字号 14-16px，颜色 `var(--text-primary)` | P0 |
| 10.1.1.4 | 与下方图标按钮保持 16-20px 间距 | P0 |

#### 10.1.2 社交媒体图标按钮

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 10.1.2.1 | 正下方并列三个图标按钮：抖音、哔哩哔哩、小红书 | P0 |
| 10.1.2.2 | 尺寸统一为 36×36px，圆角 8px | P0 |
| 10.1.2.3 | 悬停时轻微上浮（translateY(-2px)）与背景色变化 | P0 |
| 10.1.2.4 | 点击跳转：抖音 → `https://v.douyin.com/XkTzyJeCFIU/` | P0 |
| 10.1.2.5 | 点击跳转：哔哩哔哩 → `https://b23.tv/FtjgFrW` | P0 |
| 10.1.2.6 | 点击跳转：小红书 → `https://xhslink.com/m/AxkdRvT3QsH` | P0 |
| 10.1.2.7 | 跳转方式：EXE 版使用 `shell.openExternal(url)`，preview.html 使用 `window.open(url, '_blank')` | P0 |
| 10.1.2.8 | 三个图标按钮之间间距 12px，整体居中对齐 | P0 |

#### 10.1.3 图标资源

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 10.1.3.1 | 抖音图标：使用 SVG 内联，品牌色 `#000000`（深色模式 `#FFFFFF`） | P0 |
| 10.1.3.2 | 哔哩哔哩图标：使用 SVG 内联，品牌色 `#00A1D6` | P0 |
| 10.1.3.3 | 小红书图标：使用 SVG 内联，品牌色 `#FF2442` | P0 |
| 10.1.3.4 | 图标尺寸 20×20px，居中显示在 36×36px 按钮内 | P0 |

#### 10.1.4 实现位置

| 文件 | 变更说明 |
|------|----------|
| [src/renderer/pages/SettingsPage.tsx](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/renderer/pages/SettingsPage.tsx) | 在"关于"卡片内新增开发者介绍与社交媒体按钮区域 |
| [preview.html](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/preview.html) | 同步新增开发者介绍与社交媒体按钮区域（第 2474 行附近） |

#### 10.1.5 UI 布局示意

```
┌─────────────────────────────────────────┐
│ 关于                                     │
├─────────────────────────────────────────┤
│ 应用名称:     无限暖暖相册管理工具        │
│ 版本号:       v2.1.0                     │
│ 技术栈:       Electron 28 + React 18     │
│                                         │
│        开发者：纤璐不会玩摄影             │
│             （全网同名）                 │
│                                         │
│      [抖音]   [B站]   [小红书]           │
│                                         │
│ © 2026 纤璐不会玩摄影. All rights reserved.│
└─────────────────────────────────────────┘
```

### 10.2 设置页其他改进

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 10.2.1 | "清除本地数据"增加 ConfirmDialog 确认 | P0 |
| 10.2.2 | 新增扫描目录配置入口 | P1 |
| 10.2.3 | 新增快捷键自定义功能 | P2 |
| 10.2.4 | 统一 `SettingsPage` 与 `useTheme` 主题逻辑，消除重复 | P1 |
| 10.2.5 | preview.html 补全设置页（扫描选项、数据管理、关于详细信息） | P1 |

### 10.3 图库浏览增强

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 10.3.1 | 网格视图支持方向键导航 | P1 |
| 10.3.2 | 批量操作增加全选/反选按钮 | P1 |
| 10.3.3 | 排序支持组合排序（日期+名称二级排序） | P2 |
| 10.3.4 | 瀑布流视图响应式列数（JS 计算或 CSS Grid） | P2 |
| 10.3.5 | 时间线视图实现虚拟滚动 | P1 |
| 10.3.6 | 列表视图实现虚拟滚动 | P1 |
| 10.3.7 | 图片加载失败显示错误占位图标 | P1 |
| 10.3.8 | 列表视图评分星星可交互点击 | P2 |
| 10.3.9 | 重命名对话框增加文件名合法性校验（禁止 `/\:*?"<>|`） | P1 |
| 10.3.10 | 水印对话框增加实时预览 | P2 |
| 10.3.11 | preview.html 补全时间线视图和瀑布流视图 | P1 |
| 10.3.12 | preview.html 右键菜单添加复制/移动操作 | P1 |

### 10.4 详情页增强

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 10.4.1 | 图片支持缩放/拖拽（Pinch Zoom） | P1 |
| 10.4.2 | 导航支持方向键（左右切换） | P1 |
| 10.4.3 | 评分星星悬停预览 | P2 |
| 10.4.4 | GPS 坐标集成地图展示（Leaflet） | P2 |
| 10.4.5 | 详情页集成 `TagManager` 标签管理入口 | P1 |
| 10.4.6 | 删除操作增加确认对话框 | P0 |
| 10.4.7 | `toggleFavorite`/`updateRating` 实现乐观更新，避免 UI 闪烁 | P1 |

### 10.5 编辑器功能增强

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 10.5.1 | 新增"复制参数"/"粘贴参数"功能，支持跨图片应用调整 | P1 |
| 10.5.2 | 视频文件支持帧截取后编辑 | P2 |
| 10.5.3 | 全屏模式锁定 body 滚动 | P1 |
| 10.5.4 | 编辑参数自动保存草稿（localStorage） | P2 |
| 10.5.5 | `ToneCurve` 曲线插值改用单调三次样条 | P2 |
| 10.5.6 | `ToneCurve` 控制点支持双击/右键删除 | P2 |
| 10.5.7 | `ToneCurve` 控制点命中半径根据 canvas 尺寸自适应 | P2 |
| 10.5.8 | `Histogram` 支持 RGB 通道开关 | P2 |
| 10.5.9 | preview.html 编辑器滑块调整记入撤销历史 | P1 |

### 10.6 分类管理增强

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 10.6.1 | 新增分类图标选择入口 | P1 |
| 10.6.2 | 新增媒体文件归类入口（`media.category_id` 操作） | P1 |
| 10.6.3 | 分类列表增加搜索功能 | P2 |
| 10.6.4 | 拖拽增加视觉反馈（占位符指示放置位置） | P2 |
| 10.6.5 | preview.html 分类管理支持拖拽嵌套 | P1 |

### 10.7 全屏查看器增强

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 10.7.1 | 鼠标移动监听添加防抖，减少 setState | P1 |
| 10.7.2 | 支持缩放/旋转 | P2 |
| 10.7.3 | 过滤逻辑与 GalleryPage 一致，保持索引同步 | P1 |
| 10.7.4 | `handleSaveAs` 补充用户反馈 | P1 |

### 10.8 视频播放器增强

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 10.8.1 | `getVideoStorageKey` 使用文件 ID 或路径哈希，避免 localStorage key 过长 | P1 |
| 10.8.2 | 进度恢复显示 UI 提示（"已恢复至 00:30"） | P2 |
| 10.8.3 | 支持倍速播放/画面调整 | P2 |

---

## 十一、可访问性改进（P2）

### 11.1 ARIA 标签补全

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 11.1.1 | `EditorPage.tsx` 图标按钮（撤销/重做/重置）添加 `aria-label` | P2 |
| 11.1.2 | `GalleryPage.tsx` 列表视图项添加 `role="listitem"`，支持键盘导航 | P2 |
| 11.1.3 | `ToneCurve.tsx` canvas 添加 `role="application"` 与 `aria-label` | P2 |
| 11.1.4 | `ColorWheel.tsx` canvas 添加无障碍标签 | P2 |
| 11.1.5 | `Histogram.tsx` canvas 添加 `aria-label` | P2 |

### 11.2 键盘交互补全

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 11.2.1 | `ToneCurve.tsx` 鼠标交互补充键盘替代（`tabIndex`、`onKeyDown`） | P2 |
| 11.2.2 | `CompareView.tsx` 分割线拖动补充键盘支持 | P2 |
| 11.2.3 | `GalleryPage.tsx` 列表项补充 Enter/Space 处理 | P2 |
| 11.2.4 | `ContextMenu.tsx` 添加 ARIA menu 角色，支持方向键导航 | P2 |
| 11.2.5 | `ConfirmDialog.tsx` 实现焦点陷阱（`role="dialog"`、`aria-modal="true"`） | P2 |
| 11.2.6 | `FullscreenViewer.tsx` 添加 `role="dialog"` 与焦点管理 | P2 |

### 11.3 颜色对比度

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 11.3.1 | 检查 `TitleBar.tsx` 深色模式下文字对比度 | P2 |
| 11.3.2 | 检查 `var(--text-tertiary)` 小字体对比度，确保符合 WCAG AA | P2 |

---

## 十二、preview.html 专项修复（P0/P1）

### 12.1 致命 Bug 修复

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 12.1.1 | 修复第 1004 行 `const mediaFiles` 重新赋值导致文件加载完全失败 | P0 |
| 12.1.2 | 修复第 2604-2605 行水印对话框按钮重复触发（`data-action` 与 `onclick` 冲突） | P0 |
| 12.1.3 | 修复第 2209 行 EXIF 解析竞态条件 | P1 |

**问题位置：** [preview.html](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/preview.html#L1004) 第 1004 行

**风险：** `TypeError: Assignment to constant variable` 导致 `processRealFiles` 中断，UI 永久停留在 skeleton 加载状态。

### 12.2 性能优化

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 12.2.1 | 实现事件委托替代 `bindGlobalEvents` 的全量遍历 | P0 |
| 12.2.2 | 搜索输入添加防抖（200-300ms） | P0 |
| 12.2.3 | `renderApp` 前后保存/恢复 `.content-inner` 的 scrollTop | P1 |
| 12.2.4 | 像素处理移至 Web Worker 避免阻塞主线程 | P2 |
| 12.2.5 | 列表视图添加 `loading="lazy"` | P1 |
| 12.2.6 | 缓存 `getVisibleMedia` 排序结果 | P2 |
| 12.2.7 | 用 `structuredClone` 替代 `JSON.parse(JSON.stringify)` | P2 |

### 12.3 功能补全

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 12.3.1 | 补全设置页（扫描选项、数据管理、关于详细信息、开发者介绍、社交媒体按钮） | P0 |
| 12.3.2 | 添加时间线视图和瀑布流视图 | P1 |
| 12.3.3 | 分类管理支持拖拽嵌套 | P1 |
| 12.3.4 | 右键菜单添加复制/移动操作 | P1 |
| 12.3.5 | 批量导出支持格式转换（JPG/PNG/WebP）+ 质量调节 | P2 |
| 12.3.6 | 编辑器滑块调整记入撤销历史 | P1 |

### 12.4 代码质量

| 任务 | 描述 | 优先级 |
|------|------|--------|
| 12.4.1 | 提取水印面板公共渲染函数，消除 `renderWatermarkDialog` 与 `renderEditorWatermarkPanel` 重复 | P2 |
| 12.4.2 | 常量提取（maxSize、debounce 时间等） | P2 |
| 12.4.3 | 统一事件绑定方式（全部使用事件委托或全部使用 data-action） | P2 |
| 12.4.4 | 添加 JSDoc 类型注释 | P3 |

---

## 十三、执行顺序

| 序号 | 阶段 | 任务范围 | 优先级 |
|------|------|----------|--------|
| 1 | 安全漏洞修复 | SQL 注入、路径遍历、URL 校验、eval 移除、字段名注入 | P0 |
| 2 | 数据一致性修复 | INSERT OR REPLACE、事务、JSON.parse 防护 | P0 |
| 3 | 内存泄漏修复 | IPC 监听、setTimeout、Image 竞态 | P0/P1 |
| 4 | 状态管理修复 | pushHistory 闭包、EXIF 竞态、Shift 多选越界 | P0 |
| 5 | preview.html 致命 Bug | const 赋值、按钮重复触发 | P0 |
| 6 | 设置页开发者介绍 | 开发者文本 + 社交媒体图标按钮（EXE + preview.html 同步） | P0 |
| 7 | 错误处理补全 | 全局 IPC try-catch、类型安全 | P0/P1 |
| 8 | 主进程性能优化 | 同步 fs 改异步、流式响应、并发处理 | P1 |
| 9 | 架构重构 | 拆分 God Class、Repository 层、统一 IPC 返回 | P1/P2 |
| 10 | 渲染层重构 | 拆分大组件、消除重复代码、性能优化 | P1/P2 |
| 11 | UI/UX 功能丰富 | 图库/详情/编辑器/分类/全屏/视频增强 | P1/P2 |
| 12 | 可访问性改进 | ARIA 标签、键盘交互、对比度 | P2 |
| 13 | preview.html 功能补全 | 视图同步、分类拖拽、右键菜单 | P1 |

---

## 十四、验收标准

### 14.1 安全验收

- [ ] 移除 `db:query`/`db:execute` 通用 SQL IPC 通道，改为领域方法
- [ ] `media://` 协议限制路径白名单，拒绝访问白名单外文件
- [ ] `shell:openExternal` 仅允许 `http:`/`https:` 协议
- [ ] 移除所有 `eval()` 调用
- [ ] `category:update` 字段名白名单过滤

### 14.2 数据一致性验收

- [ ] 增量扫描不覆盖用户编辑字段（tags/category_id/rating/is_favorite/notes）
- [ ] `data:clear` 使用事务，清理所有相关表
- [ ] 所有 `JSON.parse` 调用有 try-catch 防护
- [ ] `category:reorder` 使用事务

### 14.3 内存泄漏验收

- [ ] 所有 IPC 监听器在组件卸载时取消订阅
- [ ] 所有 setTimeout 在组件卸载时清理
- [ ] Image 加载提供取消机制
- [ ] Scanner progress 监听器不累积

### 14.4 状态管理验收

- [ ] `pushHistory` 无闭包陷阱，快速连续操作历史记录正确
- [ ] EXIF 加载无竞态，快速切换图片显示正确信息
- [ ] Shift 多选不崩溃，空选择时行为正确
- [ ] `mediaStore.updateMediaFile` 先 await IPC 再 set

### 14.5 设置页开发者介绍验收

- [x] 设置页"关于"卡片内显示开发者介绍，居中两行文本
- [x] 文本内容："开发者：纤璐不会玩摄影（全网同名）"，字体加粗
- [x] 正下方并列三个图标按钮：抖音、哔哩哔哩、小红书
- [x] 图标按钮尺寸 36×36px，圆角 8px
- [x] 悬停时轻微上浮与背景色变化
- [x] 点击抖音跳转 `https://v.douyin.com/XkTzyJeCFIU/`
- [x] 点击哔哩哔哩跳转 `https://b23.tv/FtjgFrW`
- [x] 点击小红书跳转 `https://xhslink.com/m/AxkdRvT3QsH`
- [x] EXE 版与 preview.html 同步实现

### 14.6 preview.html 验收

- [ ] 第 1004 行 const 赋值 Bug 修复，文件加载正常
- [ ] 水印对话框按钮无重复触发
- [ ] EXIF 解析无竞态
- [ ] 事件委托替代全量遍历
- [ ] 搜索输入防抖
- [ ] 滚动位置保留
- [x] 设置页功能与 EXE 版同步（含开发者介绍与社交媒体按钮）

### 14.7 架构验收

- [ ] `Application` 类拆分为多个独立模块
- [ ] 建立 Repository 层，移除 `getDatabase()` 暴露
- [ ] 统一 IPC 返回 `IpcResult<T>` 结构
- [ ] 数据库迁移版本管理
- [ ] 引入日志系统
- [ ] 应用退出时关闭数据库

### 14.8 性能验收

- [ ] 主进程无同步 fs 操作阻塞
- [ ] 视频文件流式响应，无 OOM 风险
- [ ] 缩略图生成并发处理（4-8 并发）
- [ ] `renderPreview` 防抖 50-100ms
- [ ] 搜索输入防抖 200-300ms
- [ ] 大媒体库（万级）流畅滚动

### 14.9 可访问性验收

- [ ] 所有图标按钮有 `aria-label`
- [ ] canvas 组件有 `role` 与 `aria-label`
- [ ] 对话框有焦点陷阱
- [ ] 键盘可完全操作
- [ ] 颜色对比度符合 WCAG AA

---

## 十五、里程碑

| 里程碑 | 完成标志 | 目标版本 | 状态 |
|--------|----------|----------|------|
| M1: 安全与数据一致性修复 | SQL 注入/路径遍历/eval 移除 + INSERT OR REPLACE 修复 + 事务 | v2.1.0 | 已完成 |
| M2: 内存泄漏与状态管理修复 | IPC 监听清理 + pushHistory 闭包 + EXIF 竞态 + Shift 多选 | v2.1.0 | 已完成 |
| M3: preview.html 致命 Bug 修复 | const 赋值 + 按钮重复触发 + 事件委托 + 防抖 | v2.1.0 | 已完成 |
| M4: 设置页开发者介绍 | 开发者文本 + 社交媒体图标按钮（EXE + preview.html 同步） | v2.1.0 | 已完成 |
| M5: 错误处理与类型安全 | 全局 IPC try-catch + schema 验证 + 类型修正 | v2.1.0 | 已完成 |
| M6: 主进程性能优化 | 异步 fs + 流式响应 + 并发处理 + 超时处理 | v2.2.0 | 已完成 |
| M7: 架构重构 | 拆分 God Class + Repository 层 + 统一 IPC + 日志系统 | v2.2.0 | 已撤销（2026-06-23 审查发现重构层为死代码，已删除 ipc-router.ts/repositories/protocol/database-maintenance-service/theme-manager，承认 index.ts 为唯一实现） |
| M8: 渲染层重构 | 拆分大组件 + 消除重复 + 性能优化 | v2.2.0 | 已完成 |
| M9: UI/UX 功能丰富 | 图库/详情/编辑器/分类/全屏/视频增强 | v2.3.0 | 已完成 |
| M10: 可访问性与 preview.html 补全 | ARIA 标签 + 键盘交互 + preview.html 功能同步 | v2.3.0 | 已完成 |

---

## 十六、文件变更清单（预估）

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `src/main/index.ts` | 重构 | 拆分为多个模块，修复安全漏洞与内存泄漏 |
| `src/main/preload.ts` | 修改 | 补充 unsubscribe 返回值，移除通用 SQL 接口 |
| `src/main/file-operations.ts` | 重构 | 拆分为 FileService/VideoService/ExifService/WatermarkService |
| `src/main/database/connection.ts` | 修改 | 移除 getDatabase()，引入迁移版本管理 |
| `src/main/scanner/index.ts` | 修改 | 修复 INSERT OR REPLACE，异步 fs，并发处理 |
| `src/main/thumbnail/generator.ts` | 修改 | 修复 ffmpeg 超时与冲突，并发处理 |
| `src/main/repositories/*.ts` | 新增 | PresetRepository/CategoryRepository/TagRepository 等 |
| `src/main/services/*.ts` | 新增 | FileService/VideoService/ExifService/WatermarkService |
| `src/main/ipc/*.ts` | 新增 | 按领域分组的 IPC 路由模块 |
| `src/renderer/pages/SettingsPage.tsx` | 修改 | 新增开发者介绍与社交媒体按钮区域 |
| `src/renderer/pages/EditorPage.tsx` | 重构 | 拆分为多个组件与 hooks |
| `src/renderer/pages/GalleryPage.tsx` | 重构 | 拆分为多个组件与 hooks |
| `src/renderer/pages/DetailPage.tsx` | 修改 | EXIF 竞态修复，功能增强 |
| `src/renderer/pages/CategoriesPage.tsx` | 修改 | 功能增强 |
| `src/renderer/hooks/useImageProcessor.ts` | 修改 | 修复 Image 竞态，防抖 |
| `src/renderer/hooks/useEditHistory.ts` | 新增 | 编辑历史栈管理 |
| `src/renderer/hooks/useEditorShortcuts.ts` | 新增 | 编辑器快捷键 |
| `src/renderer/hooks/useToast.ts` | 新增 | 统一消息提示 |
| `src/renderer/hooks/useRefreshMedia.ts` | 新增 | 统一媒体刷新 |
| `src/renderer/utils/format.ts` | 新增 | formatFileSize/formatDate 等工具函数 |
| `src/renderer/utils/filter.ts` | 新增 | mergeFilterParams 工具函数 |
| `src/renderer/components/common/Toast.tsx` | 新增 | 统一 Toast 组件 |
| `src/renderer/components/editor/*.tsx` | 修改 | 拆分编辑器子组件 |
| `src/renderer/components/gallery/*.tsx` | 新增 | ListView/TimelineView/MasonryView |
| `src/renderer/stores/mediaStore.ts` | 修改 | 修复副作用混入，类型安全 |
| `src/renderer/stores/uiStore.ts` | 修改 | 添加 persist 中间件 |
| `preview.html` | 修改 | 修复致命 Bug，同步功能，新增开发者介绍 |
| `package.json` | 修改 | 新增依赖（zod、p-limit、electron-log 等） |

---

## 十七、技术实现要点

### 17.1 开发者介绍与社交媒体按钮实现

#### EXE 版（SettingsPage.tsx）

```tsx
// 在"关于"卡片内新增
<div className="flex flex-col items-center py-4 space-y-4">
  <div className="text-center">
    <p className="font-bold text-base" style={{ color: 'var(--text-primary)' }}>
      开发者：纤璐不会玩摄影
    </p>
    <p className="font-bold text-base" style={{ color: 'var(--text-primary)' }}>
      （全网同名）
    </p>
  </div>
  <div className="flex gap-3">
    <button
      className="social-btn"
      onClick={() => window.electronAPI?.shell?.openExternal?.('https://v.douyin.com/XkTzyJeCFIU/') || window.open('https://v.douyin.com/XkTzyJeCFIU/', '_blank')}
      title="抖音"
      aria-label="抖音"
    >
      {/* 抖音 SVG 图标 */}
    </button>
    <button
      className="social-btn"
      onClick={() => window.electronAPI?.shell?.openExternal?.('https://b23.tv/FtjgFrW') || window.open('https://b23.tv/FtjgFrW', '_blank')}
      title="哔哩哔哩"
      aria-label="哔哩哔哩"
    >
      {/* 哔哩哔哩 SVG 图标 */}
    </button>
    <button
      className="social-btn"
      onClick={() => window.electronAPI?.shell?.openExternal?.('https://xhslink.com/m/AxkdRvT3QsH') || window.open('https://xhslink.com/m/AxkdRvT3QsH', '_blank')}
      title="小红书"
      aria-label="小红书"
    >
      {/* 小红书 SVG 图标 */}
    </button>
  </div>
</div>
```

#### CSS 样式（globals.css）

```css
.social-btn {
  width: 36px;
  height: 36px;
  border-radius: 8px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(0, 0, 0, 0.05);
  transition: all 0.2s ease;
  cursor: pointer;
  border: none;
}
.social-btn:hover {
  transform: translateY(-2px);
  background: rgba(0, 0, 0, 0.1);
}
.dark .social-btn {
  background: rgba(255, 255, 255, 0.08);
}
.dark .social-btn:hover {
  background: rgba(255, 255, 255, 0.15);
}
.social-btn svg {
  width: 20px;
  height: 20px;
}
```

### 17.2 统一 IPC 返回结构

```typescript
interface IpcResult<T = unknown> {
  success: boolean
  data?: T
  message?: string
}

// 所有 IPC 处理器统一返回 IpcResult
ipcMain.handle('media:list', async (_, filter: MediaFilter): Promise<IpcResult<MediaFile[]>> => {
  try {
    const files = await mediaRepository.list(filter)
    return { success: true, data: files }
  } catch (error) {
    return { success: false, message: String(error) }
  }
})
```

### 17.3 Repository 层架构

```typescript
// src/main/repositories/MediaRepository.ts
export class MediaRepository {
  constructor(private db: Database) {}

  async list(filter: MediaFilter): Promise<MediaFile[]> {
    // 参数化查询，禁止拼接 SQL
  }

  async updateMetadata(id: number, updates: Partial<MediaFile>): Promise<void> {
    // 仅允许更新白名单字段
  }

  async delete(id: number): Promise<void> {
    // 事务删除，关联表级联
  }
}
```

---

*计划创建时间：2026-06-22*
*计划版本：v2.1*
*基于：全量代码审查（主进程 6 文件 + 渲染层 30+ 文件 + preview.html 3172 行）*
*审查发现：22 个 Bug + 9 个架构问题 + 13 个设计问题 + 30+ 功能改进点*

---

## 十八、开发进度记录

### 2026-06-22

#### 已完成：2.1 IPC 暴露原始 SQL 接口修复

- [x] 2.1.1 移除 `db:query` 和 `db:execute` 通用 SQL IPC 通道
- [x] 2.1.2 改为提供具体领域方法
  - 新增 `media:list` / `category:list` 用于加载媒体文件与分类
  - 新增 `media:updateNotes` / `media:updateCategory` 用于更新用户编辑字段
  - 新增 `media:delete` 用于删除数据库记录
- [x] 2.1.3 主进程内部统一使用参数化查询（新增方法均使用 `db.prepare(...).run(...)`）

**变更文件：**
- `src/main/index.ts`：移除 `db:query`/`db:execute` handler，新增领域方法 handler
- `src/main/preload.ts`：移除 `db` 对象暴露，新增 `media.list`、`category.list`、`mediaAction.updateNotes`、`mediaAction.updateCategory`、`mediaAction.delete`
- `src/renderer/vite-env.d.ts`：同步移除 `db` 类型，补充新增方法类型声明
- `src/renderer/stores/mediaStore.ts`：`loadMediaFromDatabase` 改用 `media.list` + `category.list`；`updateMediaFile` 改用 `mediaAction.updateNotes`/`updateCategory`
- `src/renderer/pages/DetailPage.tsx`、`src/renderer/components/gallery/FullscreenViewer.tsx`、`src/renderer/pages/GalleryPage.tsx`：删除数据库记录改用 `mediaAction.delete`

**验证结果：** `tsc --noEmit` 通过，`ESLint` 0 errors（7 warnings 为既有问题）。

#### 已完成：2.2 `media://` 协议路径遍历漏洞修复

- [x] 2.2.1 限制 `media://` 协议可访问路径必须在已索引的媒体目录内
- [x] 2.2.2 维护允许路径白名单，拒绝访问白名单外文件
- [x] 2.2.3 移除 `bypassCSP: true` 配置

**实现要点：**
- 在 `registerMediaProtocol` 中新增 `isMediaPathAllowed` 白名单校验
- 白名单来源：
  1. 缩略图缓存目录（`ThumbnailGenerator.getCacheDir()`）下的文件
  2. `media_files` 表中 `file_path` 或 `thumbnail` 字段精确匹配的记录
  3. 已索引媒体文件所在 `source_path` 目录下的文件
- 请求路径统一使用 `path.resolve` 规范化，防止 `../` 遍历
- 白名单外请求返回 HTTP 403

**变更文件：**
- `src/main/index.ts`：移除 `bypassCSP`，增加白名单校验逻辑
- `src/main/thumbnail/generator.ts`：新增 `getCacheDir()` 公共方法

**验证结果：** `tsc --noEmit` 通过，`ESLint` 0 errors。

#### 已完成：2.3 `shell:openExternal` URL 校验

- [x] 2.3.1 仅允许 `http:`/`https:` 协议，拒绝 `file:`、`smb:` 等危险协议

**实现要点：**
- 使用 `new URL(url)` 解析并校验 `protocol` 字段
- 非 `http:`/`https:` 协议返回 `{ success: false, message: ... }`
- 无效 URL 返回详细错误信息

**变更文件：** `src/main/index.ts` 中 `shell:openExternal` handler

#### 已完成：2.4 移除 `eval()` 执行 ffprobe 输出

- [x] 2.4.1 移除 `eval(videoStream.r_frame_rate)`，改用字符串解析 `"num/den"` 后除法计算

**实现要点：**
- 新增 `parseFrameRate(rateStr: string)` 工具函数
- 支持 `"30000/1001"` 与单数字字符串
- 非法输入返回 `undefined`

**变更文件：** `src/main/file-operations.ts`

#### 已完成：2.5 `category:update` 字段名注入修复

- [x] 2.5.1 维护允许更新的字段白名单 `['name', 'icon', 'color', 'parent_id']`，过滤 key

**实现要点：**
- `category:update` handler 中遍历 `updates` 时跳过非白名单字段
- 无有效字段时返回错误提示

**变更文件：** `src/main/index.ts` 中 `category:update` handler

**验证结果（2.3-2.5 合计）：** `tsc --noEmit` 通过，`ESLint` 0 errors。

#### 已完成：3.1 `INSERT OR REPLACE` 覆盖用户数据修复

- [x] 3.1.1 改用 `INSERT INTO ... ON CONFLICT(file_path) DO UPDATE SET` 仅更新文件元数据字段
- [x] 3.1.2 保留用户编辑字段（tags、category_id、rating、is_favorite、notes）

**实现要点：**
- 新增插入时使用默认值填充用户字段（`tags='[]'`、`category_id=NULL`、`rating=0`、`is_favorite=0`、`notes=''`）
- 冲突时仅更新文件元数据字段，不触碰用户编辑字段

**变更文件：** `src/main/scanner/index.ts` 中 `saveToDatabase` 方法

#### 已完成：3.2 `data:clear` 事务与完整清理

- [x] 3.2.1 用 `db.transaction(() => { ... })()` 包裹所有清理操作
- [x] 3.2.2 清理所有相关表（media_files、scan_history、media_tags、filter_presets、watermark_templates、edit_history）
- [x] 3.2.3 清理缩略图缓存目录

**实现要点：**
- 使用 better-sqlite3 事务一次性清理多张表
- 保留内置滤镜预设与水印模板（`is_builtin = 0` 过滤）
- 清空 `ThumbnailGenerator.getCacheDir()` 目录
- 完成后通知渲染进程刷新

**变更文件：** `src/main/index.ts` 中 `data:clear` handler

#### 已完成：3.3 `JSON.parse` 无防护修复

- [x] 3.3.1 `tag:add`/`tag:remove` 中 `JSON.parse(row.tags)` 用 try/catch 包裹，失败回退空数组

**实现要点：**
- 新增 `Application.parseTags(db, mediaId)` 私有方法统一解析 tags
- try/catch 捕获解析异常，失败时返回空数组并记录日志
- 校验解析结果是否为数组

**变更文件：** `src/main/index.ts`

#### 已完成：3.4 `category:reorder` 事务

- [x] 3.4.1 用 `db.transaction(() => { for (...) stmt.run(...) })()` 包裹

**实现要点：**
- 将 `category:reorder` 中的循环 UPDATE 包装为 `db.transaction`

**变更文件：** `src/main/index.ts` 中 `category:reorder` handler

**验证结果（3.1-3.4 合计）：** `tsc --noEmit` 通过，`ESLint` 0 errors。

#### 里程碑更新

| 里程碑 | 状态 |
|--------|------|
| M1: 安全与数据一致性修复 | 已完成 |

#### 已完成：4.1 Scanner progress 监听器累积泄漏修复

- [x] 4.1.1 在 `setupIPC` 中只注册一次 progress 监听器，注册前调用 `removeAllListeners('progress')`

**变更文件：** `src/main/index.ts`

**实现要点：**
- 将 `scannerManager.on('progress', ...)` 移至 `setupIPC` 方法
- 注册前调用 `this.scannerManager.removeAllListeners('progress')`，避免重复累积
- 扫描进度通过 `mainWindow?.webContents.send('scanner:progress', progress)` 转发给渲染进程

#### 已完成：4.2 渲染进程 IPC 监听未取消订阅修复

- [x] 4.2.1 `App.tsx` 已移除重复 `onUpdated` 监听（数据加载改为一次性初始化）
- [x] 4.2.2 `ScanButton.tsx` 保存 `onProgress` 返回的 unsubscribe，组件卸载时取消订阅
- [x] 4.2.3 `useTheme.ts` 保存 `theme.onChange` 返回的 unsubscribe，组件卸载时取消订阅
- [x] 4.2.4 `preload.ts` 中 `scanner.onProgress` / `watermark.onProgress` / `theme.onChange` / `media.onUpdated` 均返回 unsubscribe 函数

**变更文件：**
- `src/renderer/components/scanner/ScanButton.tsx`
- `src/renderer/hooks/useTheme.ts`
- `src/main/preload.ts`

#### 已完成：4.3 setTimeout 未清理修复

- [x] 4.3.1 `EditorPage.tsx`、`GalleryPage.tsx`、`CategoriesPage.tsx` 的 `showMessage` 均使用 `useRef` 保存 timer id
- [x] 4.3.2 各组件卸载的 `useEffect` 清理函数中调用 `clearTimeout`

**变更文件：**
- `src/renderer/pages/EditorPage.tsx`
- `src/renderer/pages/GalleryPage.tsx`
- `src/renderer/pages/CategoriesPage.tsx`

#### 已完成：4.4 Image 加载未提供取消机制修复

- [x] 4.4.1 `useImageProcessor.ts` 在 cleanup 中设置 `img.onload = null; img.onerror = null; img.src = ''`
- [x] 4.4.2 `FilterPanel.tsx` 同样处理 Image 竞态：cleanup 中清空回调并阻止后续 `generateThumbnails` 写入状态

**变更文件：**
- `src/renderer/hooks/useImageProcessor.ts`
- `src/renderer/components/editor/FilterPanel.tsx`

#### 已完成：5.1 `pushHistory` 闭包陷阱修复

- [x] 5.1.1 使用 `historyIndexRef` 跟踪最新 `historyIndex`，避免闭包陷阱
- [x] 5.1.2 限制历史记录上限 50 步，超出时淘汰最旧记录

**变更文件：** `src/renderer/pages/EditorPage.tsx`

#### 已完成：5.2 EXIF 加载无竞态保护修复

- [x] 5.2.1 `DetailPage.tsx` EXIF 加载添加 token 检查，组件卸载或媒体切换时取消旧请求

**变更文件：** `src/renderer/pages/DetailPage.tsx`

#### 已完成：5.3 Shift 多选越界 bug 修复

- [x] 5.3.1 `handleGridItemClick` Shift 选择逻辑增加边界检查
  - `selectedMediaIds` 为空时直接选中当前项
  - `lastIndex` 无效时直接选中当前项
  - 区间选择改用 `setSelectedMediaIds(rangeIds)`，修正类型错误

**变更文件：** `src/renderer/pages/GalleryPage.tsx`

#### 已完成：5.4 `mediaStore.updateMediaFile` 副作用混入 reducer 修复

- [x] 5.4.1 将 `updateMediaFile` 拆分为纯 reducer，仅更新本地状态
- [x] 5.4.2 新增异步 action `updateMediaFileAndPersist(id, updates)`，先 await IPC 持久化，再调用纯 reducer 更新状态
- [x] `TagManager.tsx` 改用 `updateMediaFileAndPersist`，避免 reducer 内发起 IPC 副作用

**变更文件：**
- `src/renderer/stores/mediaStore.ts`
- `src/renderer/components/gallery/TagManager.tsx`

**验证结果（4.1-5.4 合计）：** `tsc --noEmit` 通过，`ESLint` 0 errors（7 warnings 为既有问题）。

#### 里程碑更新

| 里程碑 | 状态 |
|--------|------|
| M1: 安全与数据一致性修复 | 已完成 |
| M2: 内存泄漏与状态管理修复 | 已完成 |

#### 已完成：12.1 preview.html 致命 Bug 修复

- [x] 12.1.1 修复 `processRealFiles` 中 `const mediaFiles = []` 导致后续重新赋值抛错
  - 将声明改为 `let mediaFiles = []`，允许 `mediaFiles = mediaFiles.map(applyMediaMetadata)`
- [x] 12.1.2 修复水印对话框 `文字`/`图片` 切换按钮重复触发
  - 移除 `data-action="editorWatermarkMode:*"` 与 `onclick="setDialogMode(...)"` 的双重绑定
  - 仅保留 `onclick`，由 `setDialogMode` 统一更新 `watermarkDialogState` / `batchWatermarkState`
- [x] 12.1.3 修复详情页 EXIF 解析竞态
  - 新增 `detailExifToken`，每次打开详情时自增
  - 异步解析完成后校验 token，过期结果不再覆盖当前详情 EXIF

**变更文件：** `preview.html`

#### 已完成：12.2 preview.html 性能优化

- [x] 12.2.1 事件委托替代 `bindGlobalEvents` 全量遍历
  - 在 `initApp` 中为 `document` 注册一次性的 `click`/`input`/`change` 监听器
  - 通过 `e.target.closest('[data-action]')` / `[data-input]'` 分发到 `handleAction` / `handleInput`
  - `bindGlobalEvents` 仅保留右键菜单点击停止冒泡
- [x] 12.2.2 搜索输入添加防抖（250ms）
  - 新增 `searchDebounceTimer`，输入时清空旧定时器并重新延迟设置 `searchQuery`
- [x] 12.2.3 `renderApp` 前后保存/恢复 `.content-inner` 的 `scrollTop`
  - 渲染前记录当前滚动位置，渲染后还原，避免列表/详情切换后滚动归零

**变更文件：** `preview.html`

**验证结果：** `tsc --noEmit` 通过，`ESLint` 0 errors（7 warnings 为既有问题）。

#### 里程碑更新

| 里程碑 | 状态 |
|--------|------|
| M1: 安全与数据一致性修复 | 已完成 |
| M2: 内存泄漏与状态管理修复 | 已完成 |
| M3: preview.html 致命 Bug 修复 | 已完成 |

#### 已完成：M4 设置页开发者介绍与社交媒体按钮

- [x] 10.1.1 在设置页"关于"卡片内新增固定区域，居中显示开发者介绍文本
- [x] 10.1.2 文本内容："开发者：纤璐不会玩摄影（全网同名）"，字体加粗
- [x] 10.1.3 正下方并列三个社交媒体图标按钮：抖音、哔哩哔哩、小红书
- [x] 10.1.4 图标按钮尺寸 36×36px，圆角 8px，悬停时轻微上浮与背景色变化
- [x] 10.1.5 EXE 版（SettingsPage.tsx）与 preview.html 同步实现
- [x] 10.1.6 点击图标跳转对应链接（EXE 通过 shell.openExternal；preview.html 通过 window.open 并校验 https 协议）

**变更文件：**
- `src/renderer/pages/SettingsPage.tsx`：在"关于"卡片内新增开发者介绍与社交媒体按钮区域
- `src/renderer/styles/globals.css`：新增 `.social-btn` 样式
- `preview.html`：
  - 在 `ICONS` 中新增 `douyin`、`bilibili`、`xiaohongshu` SVG 图标
  - 在 `<style>` 中新增 `.social-btn` 样式
  - 在 `renderSettingsPage()` 的"关于"卡片内新增开发者介绍与社交媒体按钮区域
  - 在 `handleAction()` 中新增 `openExternal:` 协议处理，仅允许 `http/https` 链接

**验证结果：**
- preview.html 设置页结构、文案、图标、样式与 EXE 版 `SettingsPage.tsx` 保持一致
- 社交媒体按钮链接与 EXE 版一致
- 图标尺寸、圆角、悬停动画符合验收标准

#### 里程碑更新

| 里程碑 | 状态 |
|--------|------|
| M1: 安全与数据一致性修复 | 已完成 |
| M2: 内存泄漏与状态管理修复 | 已完成 |
| M3: preview.html 致命 Bug 修复 | 已完成 |
| M4: 设置页开发者介绍 | 已完成 |

#### 已完成：M5 错误处理与类型安全

- [x] 6.1 全局 IPC 调用补充 try-catch
  - `EditorPage.tsx`：`handleExportPreset` / `handleImportPreset` / `handleSavePreset` / `handleImportPresetFromFile` 等 IPC 调用均包裹 try/catch，失败时通过 `showMessage` 提示用户
  - `GalleryPage.tsx`：`handleSaveAs` / `handleCopy` / `handleMove` / `confirmRename` / `handleDelete` / `handleBatchExport` / `handleBatchMove` / `handleBatchWatermark` 等批量与单文件操作均添加 try/catch
  - `DetailPage.tsx`：删除、收藏、评分、另存为、EXIF 加载等 IPC 调用添加 try/catch
  - `CategoriesPage.tsx`：分类创建、更新、删除、拖拽排序等 IPC 调用添加 try/catch
  - `WatermarkPanel.tsx`：水印模板保存、加载、删除等 IPC 调用添加 try/catch
  - `SettingsPage.tsx`：数据清空操作添加 try/catch
- [x] 6.2 滤镜预设结构验证
  - 主进程新增 `validateFilterPreset(preset: unknown)`，校验 `name`、`category`、`params` 字段类型与存在性
  - `editor:importPreset` 与 `editor:importPresetFromFile` 均先校验再返回，非法预设返回 `{ success: false, message: ... }`
- [x] 6.3 类型安全修复
  - `mediaStore.ts`：新增 `EditHistoryItem` 接口，将 `editHistory: any[]` 改为 `editHistory: EditHistoryItem[]`
  - `useFilterEngine.ts`：修复 `merged[k]` 因索引类型推导为 `never` 导致的 TS2322 错误
  - `useImageProcessor.ts`：统一使用 `as number` 窄化数值型滤镜参数，避免 `any` 断言
  - `imageProcessor.ts`：新增 `SizableSource` 接口，移除 `processImageData` 中读取宽高时的 `any` 类型断言，消除 4 条 ESLint `@typescript-eslint/no-explicit-any` 警告
- [x] 6.4 验证结果
  - `npm run typecheck`（`tsc --noEmit`）通过，0 errors
  - `npm run lint` 通过，0 errors，0 warnings

**变更文件：**
- `src/main/index.ts`：新增 `validateFilterPreset` 并在 `editor:importPreset` / `editor:importPresetFromFile` 中应用
- `src/renderer/pages/EditorPage.tsx`：为滤镜预设导入/导出/保存等 IPC 调用补充 try/catch
- `src/renderer/pages/GalleryPage.tsx`：为文件操作与批量操作 IPC 调用补充 try/catch
- `src/renderer/pages/DetailPage.tsx`：为详情页各类 IPC 调用补充 try/catch
- `src/renderer/pages/CategoriesPage.tsx`：为分类管理 IPC 调用补充 try/catch
- `src/renderer/components/editor/WatermarkPanel.tsx`：为水印模板 IPC 调用补充 try/catch
- `src/renderer/pages/SettingsPage.tsx`：为数据清空 IPC 调用补充 try/catch
- `src/renderer/stores/mediaStore.ts`：新增 `EditHistoryItem` 接口并替换 `any[]`
- `src/renderer/hooks/useFilterEngine.ts`：修复索引类型赋值错误
- `src/renderer/hooks/useImageProcessor.ts`：移除 `any` 断言
- `src/renderer/utils/imageProcessor.ts`：新增 `SizableSource` 接口替代 `any`

#### 里程碑更新

| 里程碑 | 状态 |
|--------|------|
| M1: 安全与数据一致性修复 | 已完成 |
| M2: 内存泄漏与状态管理修复 | 已完成 |
| M3: preview.html 致命 Bug 修复 | 已完成 |
| M4: 设置页开发者介绍 | 已完成 |
| M5: 错误处理与类型安全 | 已完成 |
| M6: 主进程性能优化 | 已完成 |

#### 已完成：M6 主进程性能优化

- [x] 9.1 同步 fs 操作改异步
  - `src/main/index.ts`：`fs.existsSync` / `fs.readFileSync` / `fs.writeFileSync` 等同步调用全部替换为 `fs.promises.access` / `fs.promises.readFile` / `fs.promises.writeFile`
  - `src/main/file-operations.ts`：复制、移动、重命名、导出、EXIF 读取等流程统一使用异步 fs API
  - `src/main/scanner/index.ts`：目录扫描、文件状态检查、游戏目录发现均改为异步
  - `src/main/thumbnail/generator.ts`：缓存目录检查、文件存在性判断、缩略图生成改为异步
- [x] 9.2 跨盘文件移动修复
  - `FileOperations.moveFiles` 使用 `fs-extra.move` 替代 `fs.renameSync`，自动 fallback 到 copy+unlink，解决跨文件系统移动失败
- [x] 9.3 `media://` 协议视频流式响应
  - `registerMediaProtocol` 中识别视频请求后通过 `fs.createReadStream` 返回 `Response`，避免大视频文件一次性读入内存导致 OOM
- [x] 9.4 缩略图与视频元数据并发控制
  - 缩略图生成引入 `p-limit`，并发数限制为 4
  - 扫描器视频元数据读取同样使用 `p-limit(4)`，避免大量 ffmpeg 并行导致系统卡顿
- [x] 9.5 ffmpeg 超时保护
  - `ThumbnailGenerator.extractVideoFrame` 与 `FileOperations.getVideoMetadata` 的 ffprobe/ffmpeg 调用添加 30 秒超时定时器，超时后 `SIGKILL` 终止进程
- [x] 9.6 主窗口安全获取
  - 移除 `this.mainWindow!` 非空断言，新增 `getMainWindow()` 方法，依次回退 `mainWindow` / 当前焦点窗口 / `undefined`，避免未创建窗口时崩溃
- [x] 9.7 孤儿缩略图清理
  - 新增 `cleanupOrphanThumbnails()`，扫描缩略图缓存目录，删除数据库 `media_files` 中无对应记录的缓存文件，释放磁盘空间
- [x] 9.8 验证结果
  - `npm run typecheck`（`tsc --noEmit`）通过，0 errors
  - `npm run lint` 通过，0 errors，0 warnings

**变更文件：**
- `src/main/index.ts`：异步化 fs 操作、视频流式响应、并发控制、ffmpeg 超时、主窗口安全获取、孤儿缩略图清理
- `src/main/file-operations.ts`：异步化文件操作、跨盘移动修复、ffmpeg 超时
- `src/main/scanner/index.ts`：异步化扫描流程、视频元数据并发控制
- `src/main/thumbnail/generator.ts`：异步化缩略图生成、ffmpeg 超时保护

#### 已完成：M7 架构重构

> **⚠️ 已撤销声明（2026-06-23 审查）**：以下 M7 设计段落保留作为历史记录，但 ipc-router.ts / repositories/ / protocol/ / database-maintenance-service / theme-manager 等重构层代码经审查确认为死代码，已全部删除。当前 `index.ts` 为唯一实现，M7 状态以"已撤销"为准。7.3/7.4/7.5/7.6/7.7 中涉及的 file-service / video-service / watermark-service / logger 等服务仍存在并使用。

- [x] 7.1 拆分 Application God Class
  - 新建 `src/main/ipc/ipc-router.ts`，按领域分组注册所有 IPC 处理器（scanner/file/thumbnail/theme/shell/settings/editor/watermark/category/tag/media/video/dialog）
  - 新建 `src/main/protocol/media-protocol-handler.ts`，独立处理 `media://` 协议、路径白名单校验、视频流式响应
  - 新建 `src/main/services/theme-manager.ts`，独立管理窗口主题与标题栏颜色
  - 新建 `src/main/services/database-maintenance-service.ts`，独立处理启动清理、缩略图修复、孤儿缓存清理
  - `src/main/index.ts` 由 800+ 行单一 Application 类精简为应用入口，仅负责服务初始化与生命周期管理
- [x] 7.2 建立 Repository 层
  - 新建 `src/main/repositories/base-repository.ts`，封装数据库连接获取与校验
  - 新建 `PresetRepository`、`CategoryRepository`、`TagRepository`、`WatermarkTemplateRepository`、`MediaRepository`
  - 各 Repository 统一使用参数化查询，替代原 `DatabaseManager.getDatabase()` 的直接 SQL 拼接
- [x] 7.3 拆分 FileOperations 类
  - 新建 `src/main/services/file-service.ts`，负责复制、移动、删除、重命名、导出、EXIF 读取
  - 新建 `src/main/services/video-service.ts`，负责视频元数据、帧截图、转码导出
  - 新建 `src/main/services/watermark-service.ts`，负责水印 SVG 生成与批量合成
  - 原 `src/main/file-operations.ts` 已完成迁移并删除
- [x] 7.4 统一 IPC 返回结构
  - 新建 `src/main/types/ipc.ts`，定义 `IpcResult<T>` 与 `IpcProgress` 类型
  - 所有 IPC 处理器统一返回 `{ success: boolean; data?: T; message?: string }`，消除原先返回值/异常/布尔三种风格混用
- [x] 7.5 应用退出时关闭数据库
  - 在 `app.on('before-quit')` 中执行 `PRAGMA wal_checkpoint(TRUNCATE)` 后调用 `dbManager.close()`
  - 确保 WAL 文件 checkpoint，避免数据丢失
- [x] 7.6 引入日志系统
  - 安装 `electron-log`，新建 `src/main/utils/logger.ts` 统一封装 info/warn/error/debug
  - 主进程关键路径（IPC 错误、协议访问、数据库关闭、扫描结果等）已切换为 `logger` 输出
- [x] 7.7 验证结果
  - `npm run typecheck`（`tsc --noEmit`）通过，0 errors
  - `npm run lint` 通过，0 errors，0 warnings

**变更文件：**
- `src/main/index.ts`：精简为应用入口，初始化 Repository、Service、IpcRouter、MediaProtocolHandler
- `src/main/ipc/ipc-router.ts`：新增，统一管理所有 IPC 路由
- `src/main/protocol/media-protocol-handler.ts`：新增，独立 `media://` 协议处理
- `src/main/repositories/*`：新增，Repository 层实现
- `src/main/services/file-service.ts`：新增，文件操作服务
- `src/main/services/video-service.ts`：新增，视频处理服务
- `src/main/services/watermark-service.ts`：新增，水印处理服务
- `src/main/services/theme-manager.ts`：新增，主题管理服务
- `src/main/services/database-maintenance-service.ts`：新增，数据库维护服务
- `src/main/types/ipc.ts`：新增，统一 IPC 返回类型
- `src/main/utils/logger.ts`：新增，electron-log 日志封装
- `src/main/preload.ts`：更新导入路径，适配新的 `src/main/types/file` 位置
- `src/main/file-operations.ts`：删除，功能已迁移至 Service 层

#### 里程碑更新

| 里程碑 | 状态 |
|--------|------|
| M1: 安全与数据一致性修复 | 已完成 |
| M2: 内存泄漏与状态管理修复 | 已完成 |
| M3: preview.html 致命 Bug 修复 | 已完成 |
| M4: 设置页开发者介绍 | 已完成 |
| M5: 错误处理与类型安全 | 已完成 |
| M6: 主进程性能优化 | 已完成 |
| M7: 架构重构 | 已撤销（重构层为死代码，已删除） |

#### 已完成：M8 渲染层重构

- [x] 8.1 拆分 `EditorPage.tsx`
  - 新建 `src/renderer/hooks/useEditHistory.ts`，管理历史栈、撤销/重做、50 步上限与闭包陷阱修复
  - 新建 `src/renderer/hooks/useEditorShortcuts.ts`，统一注册编辑器全局快捷键（F11、Ctrl+S、Ctrl+Shift+S、Ctrl+Z、Ctrl+Y、?、Escape）
  - 新建 `src/renderer/components/editor/EditorToolbar.tsx`，包含撤销/重做/重置/对比/快捷键/全屏/保存等工具按钮
  - 新建 `src/renderer/components/editor/EditorTabs.tsx`，渲染 7 个编辑标签页与对应面板
  - 新建 `src/renderer/components/editor/ShortcutsModal.tsx`，快捷键说明弹窗
  - `EditorPage.tsx` 精简为状态编排入口
- [x] 8.2 拆分 `GalleryPage.tsx`
  - 新建 `src/renderer/hooks/useFileOperations.ts`，封装单个文件的打开/编辑/另存为/复制/移动/重命名/删除/收藏/属性
  - 新建 `src/renderer/hooks/useBatchOperations.ts`，封装批量导出/移动/水印
  - 新建 `src/renderer/components/gallery/ListView.tsx`、`TimelineView.tsx`、`MasonryView.tsx` 三个独立视图组件
  - 新建 `src/renderer/components/gallery/RenameDialog.tsx` 与 `WatermarkDialog.tsx` 独立弹窗组件
  - 将 `getContextMenuItems` 提取至 `src/renderer/utils/gallery.tsx`
  - `GalleryPage.tsx` 精简为状态编排入口
- [x] 8.3 消除重复代码
  - 新建 `src/renderer/utils/format.ts`，统一 `formatFileSize`
  - 新建 `src/renderer/utils/date.ts`，统一 `formatDate`
  - 新建/复用 `src/renderer/utils/file.ts`，统一 `toFileUrl`
  - 新建 `src/renderer/utils/filter.ts`，统一 `mergeFilterParams`
  - 新建 `src/renderer/components/common/Toast.tsx` 与 `src/renderer/hooks/useToast.ts`，统一消息提示与定时器清理
  - 新建 `src/renderer/hooks/useRefreshMedia.ts`，封装 `loadMediaFromDatabase` 重复调用
  - 新建 `src/renderer/icons/index.tsx`，将多处内联 SVG 提取为类型安全图标组件
- [x] 8.4 性能优化
  - `useImageProcessor.ts` 的 `renderPreview` 添加 80ms 防抖
  - `Histogram.tsx` 同步添加 80ms 防抖
  - `GalleryPage.tsx` 搜索输入添加 250ms 防抖
  - `EditorPage.tsx` 14 个 `SliderControl` 的 `onChange` 使用 `useCallback` 包裹
  - `FullscreenViewer.tsx` 的 `filteredFiles` 使用 `useMemo`
  - `Sidebar.tsx` 将三次遍历 `mediaFiles` 合并为一次 `useMemo`
  - `uiStore.ts` 添加 `zustand/middleware/persist`，持久化 viewMode/sortBy/sortOrder/filterType 等用户偏好
  - `FilterPanel.tsx` 缩略图生成改为并行处理并限制 4 并发
- [x] 8.5 验证结果
  - `npm run typecheck`（`tsc --noEmit`）通过，0 errors
  - `npm run lint` 通过，0 errors，0 warnings
  - 修复了子代理遗留的 `CategoriesPage.tsx` 未使用导入问题

**变更文件：**
- `src/renderer/pages/EditorPage.tsx`：精简为编排入口
- `src/renderer/pages/GalleryPage.tsx`：精简为编排入口
- `src/renderer/hooks/useEditHistory.ts`、`useEditorShortcuts.ts`、`useFileOperations.ts`、`useBatchOperations.ts`、`useToast.ts`、`useRefreshMedia.ts`：新增
- `src/renderer/components/editor/EditorToolbar.tsx`、`EditorTabs.tsx`、`ShortcutsModal.tsx`：新增
- `src/renderer/components/gallery/ListView.tsx`、`TimelineView.tsx`、`MasonryView.tsx`、`RenameDialog.tsx`、`WatermarkDialog.tsx`：新增
- `src/renderer/utils/format.ts`、`date.ts`、`file.ts`、`filter.ts`、`gallery.tsx`：新增
- `src/renderer/components/common/Toast.tsx`、`src/renderer/icons/index.tsx`：新增
- `src/renderer/pages/CategoriesPage.tsx`、`LutPanel.tsx`、`CompareView.tsx`、`TitleBar.tsx`、`TagManager.tsx` 等：使用新图标组件与 useToast

#### 里程碑更新

| 里程碑 | 状态 |
|--------|------|
| M1: 安全与数据一致性修复 | 已完成 |
| M2: 内存泄漏与状态管理修复 | 已完成 |
| M3: preview.html 致命 Bug 修复 | 已完成 |
| M4: 设置页开发者介绍 | 已完成 |
| M5: 错误处理与类型安全 | 已完成 |
| M6: 主进程性能优化 | 已完成 |
| M7: 架构重构 | 已撤销（重构层为死代码，已删除） |
| M8: 渲染层重构 | 已完成 |

#### 已完成：M9 UI/UX 功能丰富 + M10 可访问性与 preview.html 补全

- [x] 10.3 图库浏览增强
  - 网格视图支持方向键导航（上下左右、Enter 打开、Space 选择）
  - 批量操作工具栏增加全选/反选按钮
  - 时间线视图实现虚拟滚动
  - 列表视图实现虚拟滚动
  - 图片加载失败显示错误占位图标
  - 重命名对话框增加文件名合法性校验（禁止 `\\/:*?"<>|`）
- [x] 10.4 详情页增强
  - 图片支持缩放/拖拽（Pinch Zoom / 滚轮缩放 / 拖拽平移）
  - 导航支持方向键左右切换
  - 详情页集成 `TagManager` 标签管理入口
  - 删除操作增加 `ConfirmDialog` 确认
  - `toggleFavorite` / `updateRating` 实现乐观更新与失败回滚
- [x] 10.5 编辑器增强
  - 新增"复制参数"/"粘贴参数"功能（localStorage + 快捷键）
  - 全屏模式锁定 body 滚动
- [x] 10.6 分类管理增强
  - 新增分类图标选择入口
  - 新增媒体文件归类入口（详情页/右键菜单/批量操作）
- [x] 10.7 全屏查看器增强
  - 鼠标移动监听添加防抖
  - 过滤逻辑与 GalleryPage 保持一致，索引同步
  - `handleSaveAs` 补充用户反馈
- [x] 10.8 视频播放器增强
  - `getVideoStorageKey` 使用文件 ID/路径哈希，避免 localStorage key 过长
  - 进度恢复显示 UI 提示
- [x] 11.x 可访问性改进
  - 编辑器图标按钮添加 `aria-label`
  - 列表视图项添加 `role="listitem"` 与键盘导航
  - `ToneCurve` / `Histogram` canvas 添加 `role` 与 `aria-label`
  - `ContextMenu` 添加 ARIA menu 角色与方向键导航
  - `ConfirmDialog` 实现焦点陷阱（`role="dialog"`、`aria-modal="true"`）
  - `FullscreenViewer` 添加 `role="dialog"` 与焦点管理
- [x] 12.x preview.html 补全
  - 修复 `const mediaFiles` 重新赋值问题
  - 修复水印对话框按钮重复触发
  - 修复 EXIF 解析竞态条件
  - 实现事件委托替代全量遍历
  - 搜索输入添加防抖
  - 列表视图图片添加 `loading="lazy"`
  - 添加排序缓存优化
  - 补全时间线视图和瀑布流视图
  - 分类管理支持拖拽嵌套
  - 右键菜单添加复制/移动操作
  - 编辑器滑块调整记入撤销历史
  - 提取水印面板公共渲染函数消除重复
- [x] 验证结果
  - `npm run typecheck`（`tsc --noEmit`）通过，0 errors
  - `npm run lint` 通过，0 errors，0 warnings

**变更文件：**
- `src/renderer/pages/GalleryPage.tsx` / `src/renderer/components/gallery/BatchActions.tsx`：全选/反选、键盘导航
- `src/renderer/components/gallery/VirtualImageGrid.tsx`、`ListView.tsx`、`TimelineView.tsx`、`MasonryView.tsx`：虚拟滚动、加载失败占位、响应式布局
- `src/renderer/components/gallery/RenameDialog.tsx`：文件名合法性校验
- `src/renderer/pages/DetailPage.tsx`：缩放拖拽、方向键导航、TagManager、删除确认、乐观更新
- `src/renderer/pages/EditorPage.tsx`：复制/粘贴参数、全屏滚动锁定
- `src/renderer/pages/CategoriesPage.tsx`：图标选择、媒体归类
- `src/renderer/components/gallery/FullscreenViewer.tsx`：防抖、索引同步、保存反馈
- `src/renderer/components/gallery/VideoPlayer.tsx`（或对应视频组件）：存储 key 哈希化、进度恢复提示
- `src/renderer/components/common/ContextMenu.tsx`、`ConfirmDialog.tsx`：ARIA 与键盘交互
- `src/renderer/components/editor/ToneCurve.tsx`、`Histogram.tsx`：ARIA 标签
- `preview.html`：多项功能补全与 Bug 修复

#### 里程碑更新

| 里程碑 | 状态 |
|--------|------|
| M1: 安全与数据一致性修复 | 已完成 |
| M2: 内存泄漏与状态管理修复 | 已完成 |
| M3: preview.html 致命 Bug 修复 | 已完成 |
| M4: 设置页开发者介绍 | 已完成 |
| M5: 错误处理与类型安全 | 已完成 |
| M6: 主进程性能优化 | 已完成 |
| M7: 架构重构 | 已撤销（重构层为死代码，已删除） |
| M8: 渲染层重构 | 已完成 |
| M9: UI/UX 功能丰富 | 已完成 |
| M10: 可访问性与 preview.html 补全 | 已完成 |
