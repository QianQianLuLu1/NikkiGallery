# 代码审计报告

**审计范围**：`src/main`（services / scanner / thumbnail / database / utils）与 `src/renderer`（stores / hooks / 关键 components）
**审计重点**：运行崩溃 → 资源泄漏 → 结构臃肿 → 冗余残留
**项目分层**：`renderer → IPC → main services → database/utils`（依赖方向单向，本次建议不破坏此方向）

---

## 一、必须修（确定会引发问题）

| # | 文件路径 | 问题 | 改动建议 |
|---|---|---|---|
| 1 | [src/renderer/hooks/useGaussianBlur.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/renderer/hooks/useGaussianBlur.ts) | `transitionTo` 启动 `requestAnimationFrame` 循环但从不取消，组件卸载后仍 `setBlurState` → 内存泄漏 + React 警告 | 用 `useRef` 保存 `animationId`，`useEffect` 清理时 `cancelAnimationFrame`；连续调用 `transitionTo` 时也先取消上一个 | ✅ 已修复：新增 `animationRef` + `cancelAnimation`，卸载 effect 清理，连续调用先取消上一个 |
| 2 | [src/main/scanner/index.ts:392-414](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/scanner/index.ts#L392-L414) | `getVideoMetadata` 超时仅 `reject(new Error(...))`，未 `command.kill()`，ffmpeg 子进程持续累积泄漏 | 参照 [video-service.ts:69-72](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/services/video-service.ts#L69-L72) 写法：保存 command 引用，超时回调内 `command.kill('SIGKILL')` 再 reject | ✅ 已修复：保存 `command` 引用，超时回调内先 `command.kill('SIGKILL')` 再 reject |
| 3 | [src/main/thumbnail/generator.ts:124-146](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/thumbnail/generator.ts#L124-L146) | `extractVideoFrame` 注册了两次 `end` 监听（外层 resolve + 内层 clearTimeout），且超时仅 reject 未 `command.kill()`；error 监听同理清理不一致 | 合并为单一 `end`/`error` 监听；超时回调先 `command.kill('SIGKILL')` 再 reject | ✅ 已修复：引入 `settled` 标志合并为单一 end/error 监听，超时先 kill 再 reject |
| 4 | [src/main/index.ts:1472](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/index.ts#L1472) | `tags: typeof f.tags === 'string' ? JSON.parse(f.tags) : f.tags \|\| []` 中 `JSON.parse` 无 try/catch，损坏的 tags 字段会让整个 `media:list` IPC 抛错、列表加载失败 | 改用已有的 [parseTags](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/index.ts#L1671-L1685) 私有方法或内联 try/catch | ✅ 已修复：抽取 `parseTagsField` 辅助函数含 try/catch，media:list 与 parseTags 均复用 |
| 5 | [src/main/index.ts:2065-2070](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/index.ts#L2065-L2070) | `rowsToRepair` 过滤条件末尾 `if (!row.thumbnail) return true / return true` —— 末尾无条件 `return true`，导致**所有记录**都被判定为需修复，启动时对已生成缩略图的记录也重复调用 sharp/ffmpeg | 末尾 `return true` 改为 `return false`，或整体改为 `return needsDimensions \|\| !row.thumbnail` | ✅ 已修复：改为 `return needsDimensions || !row.thumbnail` |
| 6 | [src/main/services/log-service.ts:83](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/services/log-service.ts#L83) | `exportLogsAsZip` 的 PowerShell 命令直接用字符串插值 `logDir` 与 `targetPath`，路径含双引号或特殊字符时命令注入或失败 | 路径用单引号包裹并转义内部单引号：`'${logDir.replace(/'/g, "''")}'`；或改用 `-LiteralPath` | ✅ 已修复：改用单引号字符串 + `''` 转义 + `-LiteralPath` 参数 |
| 7 | [src/main/index.ts:1771](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/index.ts#L1771) | media 协议处理图片用 `fs.promises.readFile` 整文件读入内存，超大原图（>100MB）会内存峰值飙升 | 改为 `fs.createReadStream` 流式响应（与视频分支一致） | ✅ 已修复：图片改用 `createReadStream`，并支持 Range 请求（206） |

---

## 二、建议改（结构不合理，改了更好）

| # | 文件路径 | 问题 | 改动建议 |
|---|---|---|---|
| 1 | [src/main/scanner/index.ts:389-414](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/scanner/index.ts#L389-L414) vs [src/main/services/video-service.ts:62-89](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/services/video-service.ts#L62-L89) | `getVideoMetadata` 与 `VideoService.getMetadata` 实现几乎一致（ffmpeg + ffprobe + 超时），重复维护 | scanner 复用 `videoService.getMetadata`，或抽到 `utils/video-probe.ts` 共享 | ✅ 已修复：抽取 `src/main/utils/video-probe.ts` 含 `probeVideoMetadata` + `VideoMetadata`，scanner 与 video-service 均复用，scanner 私有 `getVideoMetadata` 方法已删除 |
| 2 | [src/main/services/file-service.ts:40-53](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/services/file-service.ts#L40-L53) | `checkDiskSpace` 在 file-service（3 处）和 video-service（3 处）重复实现 | 抽到 `utils/disk.ts`，统一 `assertDiskSpace(dir, bytes)` | ✅ 已修复：新增 `src/main/utils/disk.ts` 含 `assertDiskSpace`，file-service 3 处调用与 video-service 3 处内联 statfs 全部替换 |
| 3 | [src/main/scanner/index.ts:353-380](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/scanner/index.ts#L353-L380) | 视频扫描任务闭包里已有 `stats`，但 width/height 又通过 `getVideoMetadata` 二次 ffprobe 读取 | `getVideoMetadata` 内部已能拿到 width/height/duration，直接合并入 `addFile` 调用，省一次 ffprobe | ✅ 已确认无需修改：闭包中的 `stats` 来自 `fsp.stat()` 仅含 size/mtime/birthtime，**不包含** width/height/duration；width/height 必须通过 ffprobe 读取，不存在二次调用，原审计前提错误 |
| 4 | [src/main/utils/logger.ts:83](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/utils/logger.ts#L83) | `fs.promises.appendFile(...).catch(() => {})` 完全吞错，磁盘满/权限问题时无任何反馈 | catch 内 `console.error('[Logger] 写入失败:', err)` 兜底 | ✅ 已修复：catch 内改为 `console.error('[Logger] 写入失败:', err)` |
| 5 | [src/renderer/hooks/useGlobalUndo.ts:21,58](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/renderer/hooks/useGlobalUndo.ts#L21-L58) | `const stack = useOperationHistoryStore((s) => s.stack)` 作为依赖，每次 push/undo 都触发 `handleKeyDown` 重建 + effect 重挂/解挂 | 改用 `useOperationHistoryStore.getState()` 在 keydown 回调内读取最新栈，依赖数组移除 `stack` | ✅ 已修复：移除 `stack`/`canUndo`/`undo` 订阅，回调内通过 `useOperationHistoryStore.getState()` 读取，依赖数组仅保留 `currentView`/`showMessage` |
| 6 | [src/renderer/components/scanner/ScanButton.tsx:7-9](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/renderer/components/scanner/ScanButton.tsx#L7-L9) | 启动后 `scanning` 本地 state 初始为 false，若主进程正在后台扫描（启动自动扫描），UI 与实际状态不一致 | 挂载时通过 `scanner:status` IPC 同步初始 `scanning` 状态 | ✅ 已修复：挂载时调用 `scanner.status()`，若返回 `status === 'running'` 则同步本地 `scanning` 与 `scanProgress`；同步更新 `vite-env.d.ts` 中 `status` 返回类型从 `unknown` 改为精确结构 |
| 7 | [src/main/services/video-service.ts:412-425](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/services/video-service.ts#L412-L425) | `buildAtempoChain` 的 while 循环无迭代上限，极端入参（如 speed=0 走到 `remaining < 0.5` 永远成立）会死循环 | 虽然上层已校验 `MIN_SPEED=0.25`，仍建议加 `let iter = 0; iter < 20` 兜底 | ✅ 已修复：加 `MAX_ITER=20` 迭代上限，循环后若仍超出范围则 clamp 到 [0.5, 2.0]，防止极端入参死循环 |
| 8 | [src/renderer/hooks/useFilteredMediaFiles.ts:24-38](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/renderer/hooks/useFilteredMediaFiles.ts#L24-L38) | 模块级 `cache` 跨组件实例共享，逻辑依赖引用比较；模式不直观，后续维护易误改 | 当前实现正确，但加一行注释说明"模块级缓存是故意的，多组件共享过滤结果"，或改为 `useMemo` 自包含 | ✅ 已修复：补充注释明确"故意为之，多组件共享过滤结果"+ 下游 store 必须返回稳定引用的约束 |
| 9 | [src/renderer/stores/operationHistoryStore.ts:30-31](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/renderer/stores/operationHistoryStore.ts#L30-L31) | `undoFn` 闭包捕获 `onRefreshMedia`、`originalPath` 等，50 步上限下闭包持有引用不释放；且未持久化，重启后撤销栈丢失（与"全局撤销"产品定位有出入） | 若产品需要跨重启撤销，需把 `payload` 持久化到数据库 `operation_history` 表（已建表但未使用），`undoFn` 改为按 type 分发 | ✅ 已修复：完整实现跨重启撤销——1) operation_history 表新增 description 列 + 旧库迁移；2) 新增 4 个 IPC handler（add/list/remove/clear）；3) store 改造：push 时异步写入数据库，undo 时从数据库删除，loadFromDatabase 启动时加载；4) undoFn 改为按 type 分发的 handler 注册表（useFileOperations 注册 4 个 handler）；5) App.tsx 启动时调用 loadFromDatabase |
| 10 | [src/main/thumbnail/generator.ts:185-200](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/thumbnail/generator.ts#L185-L200) | `loadAccessTimes` 用 `JSON.parse(raw)` 无校验，损坏的 `cache-metadata.json` 会让 `for...of` 抛错（虽被 catch 吞掉，但访问时间丢失） | 加 `Array.isArray` / `typeof v === 'number'` 守卫 | ✅ 已修复：增加 `typeof === 'object' && !== null` 守卫与 `Number.isFinite` 字段校验，损坏字段被静默过滤而非中断整批加载 |

---

## 三、待确认（拿不准是否有副作用）

| # | 文件路径 | 问题 | 待确认点 |
|---|---|---|---|
| 1 | [src/main/database/connection.ts:305-307](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/database/connection.ts#L305-L307) | `WHERE scene_category IS NULL OR scene_category = 'other'` 会把用户手动设为 'other' 的记录也按路径重新检测覆盖 | 是否符合产品预期？（'other' 既是默认值也是用户可选值，语义冲突） | ✅ 已修复：ALTER TABLE 与 CREATE TABLE 的 scene_category 默认值改为 NULL（不指定 DEFAULT），查询改为 WHERE scene_category IS NULL。首次迁移时旧记录为 NULL 会被一次性检测回填，后续启动不再对 'other' 记录重复检测，消除每次启动的重复 detectSceneCategory 调用 |
| 2 | [src/main/database/connection.ts:43-44](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/database/connection.ts#L43-L44) | `CREATE TABLE media_files` 中无 `is_deleted` / `deleted_at` / `scene_time` / `outfit` 列定义，依赖后续 `migrateXxx` 补齐；新建数据库时 schema 与实际使用存在短暂不一致窗口 | 新库启动时 `migrateXxx` 必须全部成功，否则后续 INSERT 会缺列；建议统一进 CREATE TABLE | ✅ 已修复：CREATE TABLE 统一包含 is_deleted/deleted_at/scene_time/outfit 列及对应索引，migrateXxx 保留以兼容旧库（ALTER TABLE ADD COLUMN 在列已存在时 catch 忽略） |
| 3 | [src/renderer/stores/mediaStore.ts:234-250](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/renderer/stores/mediaStore.ts#L234-L250) | `loadRecycleBin` 用 `const store = useMediaStore.getState()` 快照，finally 内 `store.setRecycleBinLoading(false)` 基于过期引用 | 当前 zustand 的 setter 是稳定的，实际无 bug；但模式不规范，建议改用 hook 订阅 | ✅ 已修复：改用 `useMediaStore.setState()` 直接更新，移除 getState 快照，消除过期引用隐患 |
| 4 | [src/main/index.ts:1902-1931](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/index.ts#L1902-L1931) | `generateThumbnailsForUnprocessed` 用实例字段 `thumbnailsGenerating` 做互斥，但启动扫描和手动扫描来自不同调用点，若任一调用方不 await 完成就退出，标志位可能锁死 | 当前所有调用点都 await，OK；建议加 `finally` 注释强调必须释放 | ✅ 已强化：代码已有 try-finally 块，补充注释明确"必须在 try-finally 中释放，否则异常退出会导致标志位锁死，后续缩略图生成永远跳过"，finally 块加注释"无论成功/失败/异常，都清除互斥标志" |
| 5 | [src/main/services/log-service.ts:84-89](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/services/log-service.ts#L84-L89) | PowerShell `spawn` 未监听 `stdout`，若输出量大可能填满 pipe buffer 阻塞子进程 | 实际 Compress-Archive 输出极少，待确认是否需要加 `child.stdout.on('data')` | ✅ 已修复：添加 `child.stdout.on('data')` 监听 stdout，失败时将 stdout 内容纳入错误消息，防御性增强 |
| 6 | [src/main/index.ts:1098-1121](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/index.ts#L1098-L1121) 等多处 | `mediaAction.updateXxx` 系列 IPC handler 内部调用 `notifyMediaUpdated()` 失效缓存并广播，但单条更新也会触发全量 `media:updated` 广播，渲染层可能频繁重载 | 万级数据下频繁广播是否有性能问题？建议改为批量操作才广播 | ✅ 已修复：notifyMediaUpdated 加 100ms 节流，高频单条更新（连续评分、标签编辑）的广播合并为一次。缓存失效立即执行（保证数据一致性），广播延迟合并（减少渲染层重载） |
| 7 | [package.json:18-31](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/package.json#L18-L31) | `dependencies` 中仍保留 `p-limit: ^7.3.0`，但代码已全部改用 `runWithConcurrency` | 已无引用，可移除（见冗余残留类）；保留会让 electron-builder 继续打包 p-limit 进 asar | ✅ 已修复：FilterPanel.tsx 中残留的 pLimit 也替换为 runWithConcurrency，package.json 移除 p-limit 依赖 |
| 8 | [src/renderer/components/gallery/FullscreenViewer.tsx:170-177](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/renderer/components/gallery/FullscreenViewer.tsx#L170-L177) | 组件卸载清理 `slideshowTimerRef` 的 effect 依赖 `[]`，但 `stopSlideshow` 通过 `isPlaying` state 控制行为；若卸载时 `isPlaying=true`，clearInterval 后 `setIsPlaying(false)` 不会触发（已卸载） | 实际上 clearInterval 已足够，setState 被吞无副作用；待确认是否需 ref 化 | ⏸ 已评估保留现状：卸载 effect 仅 clearInterval，不调用 stopSlideshow，无 setState 到已卸载组件问题。clearInterval 已足够清理资源，ref 化是过度防御 |

---

## 四、优先顺序建议

按"影响面 × 修复成本"排序，建议按以下顺序动手：

### 第一波（修崩溃/泄漏，1-2 小时）
1. **必须修 #1**（useGaussianBlur RAF 泄漏）—— 渲染层最常见的 React 警告来源 ✅
2. **必须修 #5**（cleanupAndRepairDatabase 过滤条件永远 true）—— 启动性能问题，每次开机对全库重跑 sharp/ffmpeg ✅
3. **必须修 #2 + #3**（scanner/generator 超时未 kill）—— 长期运行 ffmpeg 进程累积 ✅
4. **必须修 #4**（media:list tags JSON.parse 防御）—— 单条坏数据让整个图库加载失败 ✅

### 第二波（修安全/资源，1 小时）
5. **必须修 #6**（log-service PowerShell 路径转义）—— 命令注入面 ✅
6. **必须修 #7**（media 协议大图改流式）—— 内存峰值 ✅
7. **建议改 #4**（logger 静默 catch）—— 至少 console.error ✅

### 第三波（清理冗余，30 分钟）
8. **待确认 #7**（移除 p-limit 依赖）—— 确认无引用后从 package.json 删除 ✅
9. **建议改 #1 + #2 + #3**（video-service / disk-space / scanner stats 合并）—— 重复代码合并 ✅

### 第四波（结构优化，按需）
10. **建议改 #5**（useGlobalUndo stack 依赖）—— 减少 keydown 监听重挂 ✅
11. **建议改 #6**（ScanButton 初始状态同步）✅
12. **建议改 #7 / #8 / #10**（防御性增强）✅
13. **建议改 #9 + 待确认 #2/#3/#5**（持久化/schema/防御性）✅
14. **待确认 #1/#4/#6**（scene_category 重复检测/互斥注释/广播节流）✅
15. **待确认 #8**（评估后确认无需修改）⏸

---

## 五、影响范围提醒

- **第一波 #5（cleanupAndRepairDatabase）**：修改后启动时缩略图修复量大幅减少，需确认不会导致已损坏的 thumbnail 字段无法修复（建议同步检查 `processThumbnailForRow` 的失败重试逻辑）
- **必须修 #7（media 协议流式）**：影响所有图片渲染路径，需回归测试图库、全屏浏览、编辑器预览
- **建议改 #1（scanner 复用 video-service）**：实际采用方案为抽取到 `utils/video-probe.ts`，scanner 与 video-service 均单向依赖 utils 层，未破坏分层方向 ✅
- **待确认 #7（移除 p-limit）**：已全局 grep 确认无 `import.*p-limit` 残留，FilterPanel.tsx 也已改用 `runWithConcurrency` ✅

---

## 六、执行总结

**已完成**：全部 25 项评估完毕，24 项已修改完成，1 项评估后确认无需修改。
- 7 项"必须修"全部 ✅
- 10 项"建议改"全部 ✅（含 #9 跨重启撤销持久化改造）
- 8 项"待确认"：7 项 ✅ 已修复/强化，1 项 ⏸ 评估后确认无需修改

**验证**：
- `tsc -p src/main/tsconfig.json` 主进程编译通过
- `vite build` 渲染层构建通过
- `electron-builder --win` 打包成功

**产物**：
- 安装包：`release\无限暖暖相册管理工具 Setup 2.1.0.exe`
- 免安装版：`release\win-unpacked\无限暖暖相册管理工具.exe`

**新增文件**：
- [src/main/utils/disk.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/utils/disk.ts) —— 统一磁盘空间检查
- [src/main/utils/video-probe.ts](file:///h:/45001/Documents/WXNN%20XiangCe/wxnn-photo-manager/src/main/utils/video-probe.ts) —— 统一 ffprobe 实现

**本次会话新增修改**：
- 待确认 #1：scene_category 默认值改为 NULL + 查询改为 IS NULL，消除每次启动重复检测
- 待确认 #4：generateThumbnailsForUnprocessed 补充互斥锁释放注释
- 待确认 #6：notifyMediaUpdated 加 100ms 节流，高频更新广播合并

**评估后保留现状的项**（1 项）：
- 待确认 #8（FullscreenViewer setState 已卸载）：卸载 effect 仅 clearInterval 不调用 stopSlideshow，无 setState 问题，ref 化是过度防御
