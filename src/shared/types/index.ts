/**
 * 共享类型统一导出入口
 *
 * 主进程、渲染进程、Preload 均通过 `@shared/types` 或相对路径引用本文件，
 * 获取所有跨进程共享的类型定义。
 *
 * 设计原则：
 * - 数据库行类型与表字段一一对应（XxxRow）
 * - 渲染进程模型采用驼峰命名（XxxRecord / Xxx）
 * - 杜绝 any/unknown，全部使用严格 interface/type
 * - 所有导出均带 JSDoc 注释，便于 IDE 悬浮提示
 *
 * 使用示例：
 * ```ts
 * import type { MediaFile, MediaRow, IpcResponse, ScanOptions } from '@shared/types'
 * ```
 */

// ============================================================================
// 媒体、扫描、视频类型
// ============================================================================
export * from './media'

// ============================================================================
// 分类类型
// ============================================================================
export * from './category'

// ============================================================================
// 角色档案类型
// ============================================================================
export * from './profile'

// ============================================================================
// 编辑器、滤镜预设、编辑历史、操作历史类型
// ============================================================================
export * from './editor'

// ============================================================================
// 水印类型
// ============================================================================
export * from './watermark'

// ============================================================================
// 应用设置、备份、缓存、日志、崩溃报告类型
// ============================================================================
export * from './settings'

// ============================================================================
// IPC 通信、分享、解密、UI 主题、对话框、数据导入导出类型
// ============================================================================
export * from './ipc'
